# AdminTool API - Environment Variables Template
# Copy this file to .env for development or set these in your production environment

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
# Primary database connection (Oracle)
ADMIN_DB_PRIMARY_CONNECTION="Data Source=your-primary-db:1521/XE;User Id=ADMINTOOL;Password=your-secure-password;"

# Secondary database connection (Oracle)
ADMIN_DB_SECONDARY_CONNECTION="Data Source=your-secondary-db:1521/XE;User Id=ADMINTOOL;Password=your-secure-password;"

# ==========================================
# JWT AUTHENTICATION
# ==========================================
# JWT secret key - MUST be at least 64 characters for production
# Generate with: openssl rand -base64 64
JWT_SECRET_KEY="generate-a-secure-64-character-or-longer-secret-key-for-production-use"

# ==========================================
# SECURITY SETTINGS (Optional overrides)
# ==========================================
# Allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS="https://yourdomain.com,https://admin.yourdomain.com"

# Maximum request body size in bytes (default: 10MB)
# MAX_REQUEST_BODY_SIZE=10485760

# Rate limiting - requests per window
# RATE_LIMIT_REQUESTS=100

# Rate limiting - window in minutes  
# RATE_LIMIT_WINDOW_MINUTES=1

# ==========================================
# LOGGING CONFIGURATION
# ==========================================
# Log level (Debug, Information, Warning, Error, Critical)
# LOG_LEVEL=Information

# Log file path pattern
# LOG_FILE_PATH=logs/admintool-.log

# ==========================================
# PRODUCTION CHECKLIST
# ==========================================
# Before deploying to production, ensure:
# 1. JWT_SECRET_KEY is cryptographically secure (64+ chars)
# 2. Database passwords are stored in secure key vault
# 3. HTTPS is enforced (RequireHttps: true)
# 4. HSTS is enabled (EnableHsts: true)  
# 5. Content Security Policy is enabled
# 6. Rate limiting is appropriately configured
# 7. Log levels exclude Debug information
# 8. Database connections use encrypted transport
# 9. Firewall rules restrict database access
# 10. Regular security audits are scheduled

# ==========================================
# DOCKER ENVIRONMENT EXAMPLE
# ==========================================
# For Docker deployment, you can use:
# docker run -e JWT_SECRET_KEY="your-secret" -e ADMIN_DB_PRIMARY_CONNECTION="your-db" your-image

# ==========================================
# KUBERNETES SECRETS EXAMPLE  
# ==========================================
# For Kubernetes, create secrets:
# kubectl create secret generic admintool-secrets \
#   --from-literal=jwt-secret-key="your-secret" \
#   --from-literal=db-primary="your-db-connection"